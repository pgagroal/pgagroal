#
# DATABASE=ALIAS1,ALIAS2 USER MAX_SIZE INITIAL_SIZE MIN_SIZE
#
# No limit rules. Saturation must be reached against the global
# max_connections (set to 6 in pgagroal.conf), not against per-database
# rules: that keeps the retry-path regression for #875 testable.
